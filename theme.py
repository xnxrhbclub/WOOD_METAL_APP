"""
Палитра в духе эмблемы WOOD METAL: бронза/медь на тёмном фоне.
Цвета подобраны близко к логотипу, но чуть более насыщенные и контрастные,
чтобы хорошо читались в интерфейсе.
"""

# Фон
BG = (0.055, 0.048, 0.042, 1)          # почти чёрный, тёплый оттенок
PANEL = (0.11, 0.093, 0.078, 1)        # карточки / панели
PANEL_LIGHT = (0.165, 0.14, 0.115, 1)  # инпуты, чуть светлее панелей

# Бронза / медь
BRONZE = (0.64, 0.42, 0.21, 1)         # основной акцент (кнопки, заголовки)
BRONZE_DARK = (0.40, 0.26, 0.13, 1)    # тёмная бронза (рамки, неактивное)
BRONZE_LIGHT = (0.80, 0.58, 0.34, 1)   # светлая бронза (hover/pressed, блики)

# Текст
CREAM = (0.92, 0.87, 0.78, 1)          # основной текст
CREAM_DIM = (0.62, 0.57, 0.50, 1)      # второстепенный текст / подписи

# Статусы заказов (приглушены под общую палитру)
STATUS_OK = (0.33, 0.42, 0.27, 1)        # патина-зелёный — срок не горит
STATUS_WARN = (0.70, 0.47, 0.17, 1)      # тёплая бронза — срок близко
STATUS_OVERDUE = (0.58, 0.20, 0.15, 1)   # ржаво-красный — просрочен

FONT_TITLE = "22sp"
FONT_SUBTITLE = "15sp"
FONT_BODY = "14sp"
FONT_SMALL = "12sp"

# ---- Стеклянный (glassmorphism) слой ----
# Полупрозрачные версии панелей — используются поверх фонового изображения,
# чтобы создать эффект матового стекла в стиле iOS.
GLASS_PANEL = (0.14, 0.115, 0.095, 0.55)       # основное стекло карточек
GLASS_PANEL_LIGHT = (0.20, 0.165, 0.135, 0.60)  # чуть светлее — поля ввода, вложенные блоки
GLASS_BORDER = (0.85, 0.64, 0.38, 0.30)         # тонкая бронзовая обводка-блик
GLASS_HIGHLIGHT = (1, 1, 1, 0.05)               # верхний блик стекла
GLASS_BAR = (0.10, 0.085, 0.07, 0.72)           # верхняя/нижняя панели (более плотное стекло)
